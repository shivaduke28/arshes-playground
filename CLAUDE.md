This is a playground repository for Arshes, an iOS app for applying real-time shader effects to camera.

- use arshes mcp server
- create `.slang` file in `/shaders` directory
- do not use `code` but `file` parameters for `compile_shader` API
- use `/tmp` directory for `image` parameter output (git ignored)
